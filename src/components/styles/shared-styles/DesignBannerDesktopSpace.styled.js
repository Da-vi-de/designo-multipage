import styled from "styled-components";
import { QUERIES } from "../../../utils/mediaQueries";

export const DesignBannerDesktopSpace= styled.div`
     @media ${QUERIES.desktop} {
       margin-bottom: 2rem;
     }
`